# Current handoff — live working-tree delta

Template per [`../README.md`](../README.md). Sliding window: immediate previous
slice plus one next action only.

## Working tree

- **[P23.6I implementation-ready plan](../plans/2026-09-12-P23.6I-wall-defined-vertical-envelope.md) — Wall-defined vertical envelope (docs-only; PR #25 branch `P23.6-H`)**: the review-request proposal was ratified with corrections and replaced by a single implementation-ready plan (the proposal memo is deleted; git history keeps it). Direction: `LayoutWall.height` is the only authored vertical Wall authority (finite, positive, **no storey cap**), the canonical Floor is reduced to `{ id, name, elevation }` (no `floor.height`, and **no replacement Floor-level scalar**), Room ceilings become a derived flat plane at `floor.elevation + max(boundary Wall heights)`, `CompiledFloor.height` is deleted, and `floor.height` is consumed **once** by legacy → wall-first migration to seed migrated Wall heights. **One format cutover only — 4 → 5** (`KNOWN_LAYOUT_FORMAT_VERSIONS = [4, 5]`); the earlier memo's `P23.6I.1`/`P23.6I.2` split and its 5 → 6 → 7 ladder are **rejected** (PR #25 is unmerged, so H's Floor-capped format 5 never became a released contract, and a version bump on the writer cannot preserve the meaning of an already-persisted payload). Wall creation uses the one named `WALL_AUTHORING_DEFAULT_HEIGHT = 3` plus explicit run-level `wallChainRunHeight` state — never inference from arbitrary incident-Junction topology; Rectangle/Polygon stay uniform. Scheduling: **after P23.6H implementation and before PR #25 merges as the format-5 cutover, and before P23.6a**. H + I together are the single pre-merge vertical cutover. This **cancels** the previously deferred wall-first Floor-height editor and **supersedes P23.6H D2, D3, D5, the S4 Room/Floor-envelope clause and acceptance-gate items 2 and 4** (see H's §Supersession note). No code, schema or fixture is modified yet.
- **P23.6 in review via PR #23 (branch `p23.6`)**: whole-slice drafting visual pass — canonical `physicalWall` + `junction` selection slots on the one authority (Plan click, reconcile, Inspector panels, Junction handles, 3D-picking/tree/gizmo coverage); single-Wall toolbar (Partition button removed); segment-first chrome removal (length form, Commit button, `Committed N walls` messaging); live candidate length readout + closure cue + degenerate invalid preview; family-weighted snap markers; Room-name labels; neutral doors; diagnostic markers + Inspector topology section; `planWallRoleChange` + Defines-room-boundary control; exact Length/Angle/Thickness selected-Wall panel. Review rounds: exact numeric inputs use bounded presentation formatting (`formatMeters`/`formatDegrees`) with `step="any"` and planner-owned validity; Height editing removed/deferred (compiler derives vertical geometry from the floor — field retained as birth default); Wall/Junction hover affordances (transient, selection wins); Room labels use guaranteed-interior anchors + deterministic suppression; wall-first Room select + birth-freeze fix (tree seeding). **P23.6 merged on `main` as `3f6c78d` (PR #23).**
- **P23.6H implemented (branch `P23.6-H`)**: `LayoutWall.height` is now the authoritative physical Wall height on the one canonical path. Domain: `packages/layout-core/src/layout-wall-heights.ts` (new — `WALL_HEIGHT_EPSILON`, `canonicalWallBirthHeight()`, `validateWallHeight()`, `validateWallFirstWallHeights()`); `LAYOUT_WALL_FIRST_FORMAT_VERSION` 4 → 5 with `LAYOUT_PRE_AUTHORITATIVE_WALL_HEIGHT_FORMAT_VERSION = 4` still loadable and `KNOWN_LAYOUT_FORMAT_VERSIONS = [4, 5]`; pre-H payloads are normalized **once** at the compatible decode boundary (`normalizePreHWallFirstLayout()` + additive `migratedFromVersion: 4`), so old projects keep their previously visible floor-derived extent while canonical Save validation receives current-format state and never normalizes — and the canonical **writers** fail closed instead of migrating: `wallFirstCanonicalFormatVersionIssue()` makes `serializeWallFirstLayoutDocument()` throw and `validateWallFirstProject()` report `unsupported_format_version` for a payload still declaring the pre-H format (the tolerant validators keep accepting pre-H because the compatible decode validates before normalizing and the API publication read path loads stored v4 releases); `planWallChain`/`planWallSegment` are born at the Floor height (`WALL_CHAIN_DEFAULTS.height` removed, `closedChainWalls()` height now required, new `invalid_floor_height` rejection); the current-format codec branch applies the range rule through the shared helper. Compiler: physical Walls compile `topY = floor.elevation + wall.height` (sections, `bounds3`, cacheKey), `CompiledPhysicalWall` gains `height`, Room/Floor envelopes stay Floor-derived. Openings: `validateWallFirstOpeningSet()` fits the hosting Wall (`opening_exceeds_floor_height` → `opening_exceeds_wall_height`) and the shared core validator gained the optional `openingHeightLimitBySegmentId` (wall-first adapter populates it; legacy callers unchanged). Operation/UI: `planExactWallHeight` (+ `wall_height_below_opening` mapping), `updateWallFirstWallHeight`, planner-backed Inspector `Height (m)`. Parity: `buildStandaloneWallMesh(wall, floorElevation, options)` derives the extent from `wall.height` in both byte-identical builder copies (all three existing call sites dropped the ceiling argument) and the editor-app `LayoutMuseumShell.svelte` gained the canonical `geometry.walls` → `buildStandaloneWallMesh` path (plus the empty-Room-detail skip) it was missing. Tests: `p23-6h-wall-height.test.ts` (34), `p23-6h-height-edit-history.test.ts` (7), `p23-6h-height-parity.test.ts` (9); P23.6's Height-deferral assertion and the format-4 test literals reconciled (canonical-current fixtures moved to `LAYOUT_WALL_FIRST_FORMAT_VERSION`; only pre-H compatibility fixtures keep a literal `4`). Full editor suite 229 files / 3151 tests green (+1 pre-existing skip); both app `check`s, layout-core/project-model typechecks, API tests and the museum visitor-bundle verification pass. **Review round (PR #25)**: the generic project writer is now gated too — `wallFirstCanonicalProjectFormatIssue()` (project-model) is applied inside `serializeProject()` (the serializer the editor facade re-exports) and inside the editor's `writePendingCloudSave()` session handoff, so no project writer can emit a pre-H payload; the gate applies only to a Layout that *declares* a format version, so version-less legacy (Room-owned) layouts stay writable and the API publication read path keeps loading stored v4 releases through the tolerant `validateProject()` (`packages/api` publish-path gating remains P23.7's). Deferred and unchanged: canonical 3D wall-first Wall/Opening picking/highlighting, wall-first Floor-height editing.
- **P23.6H / P23.6a / P23.6b implementation-ready plans (docs-only, merged via PR #24 as `f1d13c4`)**: reconciled against `main` `3f6c78d`. `docs/plans/2026-09-12-P23.6H-vertical-wall-semantics.md` (authoritative `LayoutWall.height` — compiler/bounds/mesh/Opening-fit + editor/visitor parity, historical pre-H compatibility, one exact Height operation; its `floor.height` birth default, cap and Floor-derived Room/Floor envelope are superseded before merge by P23.6I); `docs/plans/2026-09-12-P23.6a-wall-first-room-unit-move.md` (rigid isolated Room-unit X/Z move, explicit boundary-lineage Room correspondence, associated-LayoutObject policy, gesture lifecycle, one history entry); `docs/plans/2026-09-12-P23.6b-architecture-hierarchy-inspector-reconciliation.md` (ownership-truthful hierarchy + selection-first Inspector; retires the Inspector-local `precisionTarget`). The old `2026-09-11-P23.6a-architecture-inspector-hierarchy.md` seed is superseded and removed (`P23.6a` now means Room Unit Move). Order: P23.6 → P23.6H → P23.6I → P23.6a → P23.6b → P23.7 (H + I are one pre-merge format-5 cutover; I adds no schema generation). Review-correction pass: H owns bringing `apps/editor/src/lib/museum/layout/LayoutMuseumShell.svelte` onto the canonical `geometry.walls` → `buildStandaloneWallMesh(...)` path (it currently renders Room meshes only, so wall-first canonical Walls never render there; the standalone `apps/museum` copy already has the path). H's pre-H Wall-height normalization is pinned to the single compatible read/decode boundary (canonical Save validation never normalizes). B keeps `layoutSelectionAncestorRoomId` and adds a sibling `layoutSelectionRevealTarget`; the new document-level Layout Objects / Scene Content roots are populated for wall-first documents only (legacy keeps its Room-nested projection, so nothing renders twice); wall-first Room rows cannot reach the legacy `updateLayoutRoomFields` rename. Canonical wall-first 3D Wall/Opening picking + highlighting is now actually registered in the umbrella's Deferred capability register (owner unassigned) and restated in P23.7.
- **P23.5 implementation complete on PR #22 (`c83085d`)**: Column/Platform/Plinth ship as creation defaults over ordinary document-level LayoutObjects — no preset kind, no preset metadata, no Room containment. Domain: `LAYOUT_ARCHITECTURAL_PRESETS` + `planCommitLayoutObjectPreset` in `packages/layout-core/src/layout-wall-first-precision.ts` (new `layout-object-preset-create` precision operation, deterministic `layout-object-N` ids, floor-relative placement, canonical candidate gates). Interaction: `preset-column | preset-platform | preset-plinth` tools + hover-only `presetDraft` (excluded from `hasLayoutTransientInteraction`, like the wall-chain cursor). Preview: `commitLayoutObjectPreset` (one ordinary object; rejection installs nothing). Plan: footprint ghost in `plan-overlays.ts` (`presetIdForTool`). UI: wall-first-gated toolbar buttons + Inspector Place accordion; viewport one-click commit at the snapped point (tool stays armed, Escape disarms), hover footprint, pointerleave/cancel clearing. Partition is explicitly not a preset (first-class P23.9 Walls only). Tests: `apps/editor/tests/lib/layout/p23-5-presets.test.ts` (17 tests: table contract, ordinary-object commits, elevation without Floor ownership, deterministic ids + input purity, rejections, P23.1 edit/delete parity, compile/render parity vs manual primitives, Save round-trip with no `preset` bytes, hover-draft guard exclusion, preview commit + reject-leaves-untouched, history acceptance — one click is exactly one entry with exact Undo/Redo identity, reject writes zero, Escape writes zero).
- **P23.3 merged on `main` via PR #19 (`d1705b7`); P23.9 via PR #18 (`5f20aaa`); P23.4 via PR #21 (`c6601f2`)** — back-pointer; order lives in the tracker.
- **P24 planning**: [R9 minimum freeze](../plans/2026-09-10-P24-R9-minimum-freeze.md) and [P24.0–P24.5 child briefs](../plans/2026-09-10-P24-minimum-child-plans.md) complete the planning reconciliation. PR #17 review amendments define selection-independent Gallery project context, canonical supplied-material IDs/type evolution, and the pinned common-subset performance reference. Documentation only; no product/schema/dependency changes or runtime acceptance. Previous: [R8 presentation reconciliation](../plans/2026-09-10-P24-R8-presentation-reconciliation.md). Next on this track: owner review of the concrete freeze/briefs; execution remains blocked on accepted P23 minimum and approval.
- **P23.2 is on `main` via PR #13 (`a369a15`)**. The implementation is committed as `87e20c` on `main` plus review-round commits `cfd93be`/`c0a3874` (round 1, typed-owner snap exclusion, negative-slope-safe wall-span merge, collision-safe `kind:id` align reference keys, offset-space opening-drag resolver), `d3a09ce` (round 2: compiled `wallKey` wall identity, curved-wall nearest-point semantics, intersection self-exclusion), `45ce7f0` (round 3: collision-safe `geometryId` wall identity, typed `snapOwnerKey` ownership, relative straightness tolerance), and `889e4d3` (round 4: owner-aware coincident-junction dedupe and `ownerId` in stable tie-breaks). Snap suite 43 / align suite 18 / geometry compiler 19; full editor gate green (215 files / 2880 tests + 1 pre-existing skip).** Foundations:
  - `packages/layout-core/src/layout-snap.ts` (new, ~500 lines): centralized deterministic P23.2 snap resolver — `LAYOUT_PLAN_GRID_STEP = 0.25`, `LAYOUT_PLAN_SNAP_RADIUS_CSS_PX = 8`, `snapAcquisitionRadiusWorld` (zoom-stable CSS-px to world conversion), semantic ranks (`junction` 0, then `wall-intersection`, `wall-midpoint`, `opening-edge`, `wall-span`, object-bounds edge/center, guides, `grid` 9), `pickSnapWinner` (rank then screen distance then stable key; never iteration order), per-family candidate builders (span endpoints/midpoint/nearest-span, opening edges, rotated object footprint edges/center, orthogonal guides, wall-wall intersections via `classifyWallIntersection` T/crossing classes only), and `resolveLayoutSnap(geometry, point, ctx, input)` over `CompiledLayoutGeometry.queries` (dedupes per-sample wall spans to full segments). Pure — no mutation, no persistent snap points, no history.
  - `packages/layout-core/src/layout-align.ts` (new, ~200 lines): pure `planLayoutObjectAlign(geometry, objectId, reference, action, axis)` — one object to one reference (`object` footprint AABB / `room` boundary-span AABB / `wall` compiled span), actions `min|center|max` per X/Z axis plus `center-on-wall` (projects footprint center onto the canonical wall span). Returns a position patch preserving Y/rotation/dimensions/roomId; `no_op` rejection when already aligned so callers add no history.
  - `packages/layout-core/src/index.ts`: exports both modules.
  - `apps/editor/tests/lib/layout/p23-2-snap.test.ts` (new, 43 tests, all passing): constants, zoom-stable radius at 50/100/200%, winner order incl. semantic-beats-grid and stable-key tie-breaks, exclusions, all candidate families, grid fallback, resolveLayoutSnap over compiled query geometry, negative-slope + curved + legacy room-qualified wall identity, coincident distinct-owner junctions under both query orders, and the offset-space opening-drag resolver.
- **P23.1 is merged on `main` through PR #9 (`d5ec0df`)**; P23.2 is merged through PR #13 (`a369a15`); P23.9 is merged through PR #18 (`5f20aaa`); P23.3 is merged through PR #19 (`d1705b7`). The current documentation focus is the P23.6/P23.7 single-Wall UX reconciliation. The prior separate `codex/p23-implementation` implementation track is no longer the live source of truth for P23.1 status.
- **P23 Foundation Gate F0 is CLOSED on `main` as of 2026-09-10**: PR #7 merged as `32b2e8f`. Stage-6 writer flip is `455f587`; cross-format transaction-invariant restoration is `93111a2`; final proof tightening is `79b3551`. `LAYOUT_MUTATION_POLICY['wall-first']` is `adapted`, its refusal reason is retired, and layout commits explicitly require begin format = live format = candidate format with all three adapted.
- **Stage-6 safety invariant is regression-pinned**: legacy→wall-first and wall-first→legacy mid-transaction swaps refuse and close the bracket; wall-first→wall-first commits normally using a real `project.layout` snapshot; adapted→unrecognized refuses; and a legacy begin/live host with a wall-first candidate snapshot refuses, directly pinning the candidate-format leg. `cancelLayoutTransaction`, `clearSharedHistory`, and `importDocument` clear the captured begin format so stale transaction state does not survive bracket/document boundaries.
- **F0 acceptance passed 2026-09-10 before the writer flip** (end-to-end: full Vitest gate + `layout-core`/`project-model` tsc + editor/museum `svelte-check` + agent-browser smoke on `/`, `/project/:id/spatial` incl. Plan/3D + Preview takeover/exit, `/museum`, `/museum/editor`; console shows only the known `cameraPlan` `ownership_invalid_mutation` warnings). The stage-6 branch subsequently recorded a green full gate, and the final proof-only commit had a green Vercel deployment before merge.
- **F0 review fixes committed 2026-09-09 on `main`** (`442f85a`, pre-flip must-fix + should-fix): scene `unrecognized` fail-closed + scene commit re-check with commit-swap tests (stage 1); sweep regression tests + directional-light omit fix + node-guard unification (stage-2 probe: claimed waypoint/anchor/keyframe hole did NOT reproduce — post-parse sweep already rejects); rotation-with-direction + survivor/reverse/edge subdivision + full-layout Save identity (stage 3); legacy composer-vs-composer parity + true-compat serialize identity (stage 4); connection-path adversarial passthrough + portal path unification + `faceKey` rejection field (stage 5); plan/code name-drift corrected. Historical note: at this commit all writes were still disabled; stage 6 later enabled wall-first writes via PR #7.
- **P23.0 F0 stage 5 committed 2026-09-09 on `main`** (`0d66aed` (named small items): standalone Scene import `importStandaloneSceneDocument` (world-local direct, legacy + explicit frame mapping converts once, missing/incomplete/invalid mappings reject by name); portal Save-blocker `validateWallFirstPortalRelations` wired into `validateWallFirstProject` as `nonadjacent_portal_relation` plus a non-blocking migration diagnostic for carried relations (door-only/distinct-shape rules stay codec-owned after a dead-code review); no-second-transform adversarial regression (legacy-layout + world-scene) through resolve plus both composers. Fixed en route: stage-3 portal round-trip fixture related independent rects through a non-shared wall, now births a shared-wall pair.
- **P23.0 F0 stage 4 committed 2026-09-09 on `main`** (`2c9b04d` (visitor parity + cold-load proofs): one shared `prepareCompatibleRuntime` adapter (`packages/project-model/src/compat-runtime.ts` + `createEmptyLayoutRoomRegistry`) now fronts both the Preview composer (`preview-coordinator.ts`) and the visitor cold path (`visitor-cold-runtime.ts`). Legacy render-model preflight preserved for legacy-compatible inputs. Fixed en route (P23.0b defect): `decodeProjectCompatible` threw uncaught on legacy scenes with dangling roomId refs during migration conversion, now fails closed to legacy-compatible with diagnostics.
- **P23.0 F0 stage 3 committed 2026-09-09 on `main`** (`e57b1c3`, test-only slice, no product code): P23.8 defaults/allocation/replay fixture suite against the stage-2 writers — `apps/editor/tests/lib/editor/p23-f0-stage3-writer-fixtures.test.ts` (18 tests: birth identity/defaults/canonical order/wall-order cycle-equivalence/partial-flip rejection/tiny birth; object preserve/dangling-reject/replay/input-immutability; subdivision integration birth→`planWallSplit`→1→1 with ID preserved + opening rebase + interior-split rejection + ID replay; portal perimeter-survive/unknown-relation-reject/born-relation Save round-trip; born-content Save byte-identity). One expectation corrected en route (boundary start offset rotates with input wall order; ID/name/wall-set stay canonical — test asserts cycle equivalence, no product change).
- **P23.0 F0 stage 2 committed 2026-09-09 on `main`** (`b5427d8`: canonical writers for the P23 minimum, all writes still disabled): authoring planners `planFirstEnclosureRoomCreation` + `planPartitionToBoundaryRoomBirth` over the P23.8 engine (`packages/layout-core/src/layout-wall-topology-ops.ts`), canonical wall-first Save writer (`packages/project-model/src/wall-first-project.ts`), package manifest `formatVersion: 2` (`PACKAGE_MANIFEST_FORMAT_VERSION`, old manifests stay importable). Stage status recorded in the P23.0 plan's F0 execution order addendum.
- Fixed en route (latent P23.0b defect): world-local Scene decode failed on every document with entities/navigation nodes — parser guards treated a legally absent `roomId` as failure and the canonicalizer re-serialized `roomId: undefined` keys. Guards are mode-aware now; present roomIds still reject (`room_id_forbidden_in_world_local`). `scene-codec/canonical.ts`, `parse-document.ts`, `parse-entities.ts`.

## Next action

- **Implement P23.6I** (`docs/plans/2026-09-12-P23.6I-wall-defined-vertical-envelope.md`, `implementation-ready`) — statuses and supersession notes are written; the plan's stages I1–I7 are the executable order. Land it **on the `P23.6-H` branch before PR #25 merges**, so the format-5 cutover reaches `main` once as H + I. Sequence: P23.6 → P23.6H → **P23.6I** → P23.6a → P23.6b → P23.7.
- **P23.6 merged on `main` through PR #23 (`3f6c78d`)**; the next live work is the docs-only P23.6H / P23.6a / P23.6b implementation-ready plans on branch `P23.6-H-a-b-docs-update`, then implementation in dependency order P23.6 → P23.6H → P23.6I → P23.6a → P23.6b → P23.7 (H + I are one pre-merge format-5 cutover). CAUTION for agents in this repo: file-edit tool calls write to the MAIN checkout (`/Users/tony/Documents/Biskiq/spatial-sketch-editor`) regardless of shell cwd — verify every write lands in the active worktree and move/clean stray files in the main checkout before finishing.

## Verification

- P23.5 worktree gate 2026-09-11 (rebased on `c6601f2`, review fixes): editor Vitest 222 files / 3039 tests passed (+1 pre-existing skip, incl. new `p23-5-presets` 17), `layout-core` + `project-model` tsc clean, editor + museum svelte-check 0/0.
- F0 acceptance 2026-09-10 on clean `main` (`442f85a`): Vitest 211 files / 2801 tests passed (+1 skipped file/suite, pre-existing), `layout-core` + `project-model` tsc clean, editor + museum svelte-check 0/0. F0-direct suites: stage-1 format-policy 12, stage-2 writers 26, stage-3 fixtures 21, stage-4 visitor-parity 14, stage-5 small-items 15. Agent-browser smoke (dev `:5173`): `/` entry, `/project/:id/spatial` (Plan SVG + 3D 1 canvas + hierarchy/inspector), Preview takeover + exit, `/museum` Chopin guided relic, `/museum/editor` frozen relic — console shows only known `cameraPlan` warnings.
- Stage-6 branch full gate (recorded before final proof-only commit): Vitest 211 files / 2801 tests (+1 skipped, pre-existing), `layout-core` + `project-model` tsc clean, editor + museum svelte-check 0/0. Final proof-only commit `79b3551` added candidate-format regression coverage and had a green Vercel deployment before merge.
- New coverage: `apps/editor/tests/lib/editor/p23-f0-stage3-writer-fixtures.test.ts` (18 tests — birth defaults/canonical allocation/cycle-equivalence/partial-flip + tiny-birth identity, object preserve/dangling-reject/exact-replay/input-immutability, subdivision birth→split→1→1 with ID preserved + opening rebase + interior-split rejection + split ID replay, portal perimeter-survive/unknown-reject/born-relation round-trip, born-content Save byte-identity); stage-2 `p23-f0-stage2-writers.test.ts` (26 tests) still green.

## Known bugs / deferred

- **P23.9 committed walls do not render — owner: P23.6 (architectural drafting visual pass).** Reproduced live 2026-09-10 on a wall-first document: two anchors + `Finish wall chain` reports `Committed 1-wall chain` and the document really contains `wall-chain-1` (junction-chain-1 → junction-chain-2), yet the plan draws nothing and still shows the `Empty floor plan` ghost with 0 geometry warnings. Cause: the shared compiler input is room-only (`CompilerSource.floors[].rooms[]`, `packages/layout-core/src/layout-geometry.ts`), and `compileWallFirstLayoutGeometry` derives every wall segment from `room.boundary`, so a Wall no Room references never reaches the compiled geometry; `buildPlanRenderModel` then emits wall primitives only by iterating `room.walls` (`apps/editor/src/lib/layout/plan-render-model.ts`). Such a wall is invisible to Plan, snap/align queries and 3D alike, with no diagnostic. P23.6 owns the Plan wall-first Wall/Partition hierarchy and must extend the compiled geometry to emit Walls independently of Rooms — recorded here as an explicit carve-out, since P23.6 as written is presentation-only (`LayoutDocument → compileLayoutGeometry() → render data`) and the room-only emission lives upstream in the compiler. Until then P23.9's "committed Walls visually distinct from draft" bullet is unmet — a closed chain that encloses a Room does render, an open chain does not.
- **DEFERRED — the new-project Layout format is recorded as the FINAL P23 GATE and is an unmade product decision, not a slice deliverable.** `createEmptyWallFirstLayoutDocument` records that "the editor's default boot document remains the legacy compatibility fixture", and no user-visible migration is wired (`migrateLegacyLayoutDocument` is unused in `apps/editor`). The P23 umbrella says the stage-6 flip left "legacy room-owned behavior… only as the compatibility read path", which the legacy boot contradicts, while P23.7 assigns migration to system behaviour rather than a user command. The decision — (a) boot new projects wall-first, (b) migrate each legacy document on first wall-first contact (auto up-conversion establishing the history baseline), or (c) keep the dual-format editor and the per-format tool gating — must be made and executed **before P23.7 closeout declares P23 done**, because P23.7 requires "wall-first authoring… through one canonical Layout compiler/runtime path" and Publish from the canonical writer. Until then: wall-first documents gate Door/Window/legacy-primitives off (P23.3 owns canonical openings), legacy projects gate Wall/Partition off, and Rect/Polygon commit canonically only on wall-first documents (on legacy documents they stay legacy room-polygon tools).
- **Format-gated tools (expected transition).** A brand-new project boots the legacy fixture — Rect Room / Poly Room / Door / Window enabled, Wall / Partition disabled with a reason. On a wall-first document the inverse holds, except Rect Room / Poly Room stay enabled: P23.9 reimplemented them as convenience frontends that commit the same canonical Junction/Wall chain (rect ≡ four-wall chain is regression-pinned to exact-document equality). Door / Window remain disabled on wall-first documents because the legacy `Opening → Room-owned segment` model is compatibility-only after F0; owner is P23.3 ("Wall-hosted openings that fit", status `implementation-ready`, explicitly *blocked by P23.9 Wall authoring* — now unblocked pending P23.9 merge). Interim path for wall-first editing is the Inspector's `Architecture · exact` panel, which exposes Junctions/Walls/Rooms only — so there is **no way to author an opening on a wall-first document** until P23.3.
- **P23.9 roomless-wall post-commit selection deferred — owner: P23.6/P23.7 (no plan amend).** The plan's recommended minimum asks a one-segment chain to select its created Wall, but the live selection authority is still legacy room-anchored (`{kind:'wall', roomId, segmentId}`, `layout-interaction.ts:121-124`) with no `wallId` target; inventing one here would violate the no-second-selection rule, and P23.8:371-381 already requires the `(roomId, segmentId)` cutover to canonical `wallId`/`junctionId` keys. Implemented now: a chain that births a Room selects it through the existing authority; a roomless chain keeps the prior selection and reports the commit on the plan surface. Strict wall-select behavior ships with the selection-authority cutover, not in this slice.
- Visitor preview with unlit content renders near-black (not a bug — the scene
  has no lighting and the lighting system is lackluster by design at this
  tier; orbit + lit geometry verified working). Lighting expands in P23/P24.
- Camera-connections `ul[role=tree]` holds a bare `li.unused-row` (axe critical + serious pair). Minor; suggested ride-along.
- P3.4/P3.5 remain undone/not accepted and low-priority deferred.
- Direct 3D wall/interior-anchor picks remain deferred.
- Layout hover feed and anchor-helper octahedra remain disconnected.
- Drafted-room `focusRoom` retains a latent Paris-default path outside the fixed editor flow.
- Runtime logs retain known Svelte `ownership_invalid_mutation` warnings for `cameraPlan` and `layoutInteraction`; static checking is clean.
- A browser axe audit still reports generic editor color-contrast review items in empty/status text and SVG labels; these are outside the closed P3B gate.
- Deployed Render/Neon-topology smoke is deferred to the publish tier or an owner-scheduled pass (local P19/P20 smokes passed 2026-09-03/04).

## Traps

- Track-vs-raised fills measure 1.05–1.12 (borders carry the edge); Slice 4 holds the Slice 1.1 calibration by decision — no new track hexes without owner eye-test.
- Both Plan workspaces stay mounted. Hidden cells retain `inert` and `plan-cell--hidden`; shared `view` remains one Plan|3D axis.
- Camera 3D rig unmount during a main-editor Camera Plan switch must preserve the paused preview session. Leaving the Camera workspace stops it through `setWorkspace`; the relic retains stop-on-unmount/stop-on-Escape behavior.
- P12 ordinary selection never enters Camera/Edge scope. Only explicit preview actions do; sequenced-node selection in Sequence seeks + pauses.
- Camera timeline edge keys include direction; preview-route memo keys on `preview.runId`, never cloned route identity.
- Camera means guided PerspectiveCamera navigation, never webcam.
- agent-browser `press` keys land unreliably unless the target holds focus; window-dispatched `KeyboardEvent` via `eval` exercises the same handler path deterministically. Never hold CDP mouse-down across shell calls — it blocks the channel until `up`.
- Public `/p/:publicationId` load `$effect` must not track `bundle`: dispose it via `untrack(disposeBundle)` — assigning the loaded bundle retriggers a tracking effect into an infinite refetch/WebGL loop (P22.5 lesson, regression-pinned).
- Direct curl POSTs without the editor `Origin` header get 403 from the API CSRF guard before routing; send `Origin: https://spatial-sketch-editor.vercel.app` when probing production route existence (test-auth 404 check).
- World-local Scene documents must never carry `roomId` anywhere (entities, nodes, clusters, anchors, waypoints, view keys); the codec rejects a *present* key by name but treats an *absent* key as legal — don't "restore" the old `roomId === undefined` guards.

## Non-negotiables

- `/museum` is visitor-only and its chunks contain no editor/layout code. Editor ships at `/`, `/editor`, and frozen relic `/museum/editor`.
- No commits unless the user asks.
- One nav + one motion: `@portfolio/camera-core` owns `camera-route.ts` + `camera-motion.ts` only.
- Svelte 5 runes / Threlte; no second selection, history, graph, motion, geometry, or transform system.
