# P23.4 — Duplicate and linear repeat (child plan)

**Parent:** [P23 — Layout Depth umbrella](2026-09-07-P23-layout-depth-minimum-build.md)
**Status:** `evidence complete — reconciliation pending` (no gating harvest — existing research + verified repository seams are sufficient; H1 conclusions are optional context only. Reconcile against the live repository, then mark `implementation-ready`.)
**Repository rechecked:** 2026-09-08 (P23 ground-truth review pass)

Seed preserves implementation detail migrated from the umbrella. Uncertainty is
low (pure document operations over verified seams): existing research covers
this operation class, so no harvest gate applies. Reconcile against the live
repository and flip to `implementation-ready`; H1 conclusions (§Evidence plan)
are optional context for clone/remap lessons.

## Parent contract (durable, from umbrella)

Supported targets: one authored non-profile layout object, one opening on its
current segment, or one room with its owned supported layout objects. Copies are
independent canonical records. Duplicate = repeat with one copy. Repeat adds an
integer 1–50 copies; the cap is deliberate, raised only with measured evidence.
Reject room-copy requests containing read-only profile objects or external door
relations; never silently strip them. One commit, one Undo for the whole batch.

## Current implementation (verified seams)

- `layout-object-editing.ts` — object creation/patches and ID allocation
  conventions.
- `layout-opening-editing.ts` — opening patches on their owning segment.
- `layout-room-transform.ts` — room-unit clone context (owned objects travel
  with the room frame).
- `layout-mutation-runner.ts` / `layout-transaction.ts` — begin/commit/cancel,
  one chronological tagged history stack.
- `packages/layout-core/src/layout-types.ts` — ID fields across rooms/objects/
  segments/openings/interior anchors; profile kind.

## Gap

No duplicate/repeat operation exists for any supported target; batch candidate
validation and atomic rollback do not exist yet.

## Preserved implementation detail (pre-evidence seed)

### Operation semantics

- Creator supplies translation X/Z for objects/rooms, or spacing in meters
  along the segment for openings. All copies generate from the original using
  `index × delta`, not accumulated rounded offsets.
- Allocate unique room/object/segment/interior-anchor/opening IDs and remap
  internal references. Preserve object Y/rotation/dimensions and room
  floor/frame/shape.
- Room copies include owned Layout structure, never furniture, Scene entities
  or camera tours; state this beside the action.
- Reject room copies containing read-only profile objects or external door
  relations until the creator removes them; never silently strip or link cloned
  rooms back to originals. Standalone opening copies likewise reject linked
  doors.
- Preview the entire candidate and validate once as a batch. Any invalid copy
  rejects the whole operation with the failing target/reason. One commit, one
  Undo. Select the first new top-level target after success; no multi-selection.

### Two-pass clone shape (openPlan3D-derived, to reconcile)

- Research pattern (T10/T11 lineage): two-pass collect in stable order →
  allocate IDs → old→new map → clone → remap → transform → validate → commit
  once. Never `Date.now()` IDs (openPlan3D documented anti-pattern). Scene
  stays byte-equivalent (T11).

## Evidence plan

1. Optional context read: `P23-H1` clone/remap lessons once complete.
2. Re-run the recheck against the then-current repository (mutation-runner
   commit-failure semantics per the umbrella's operation contract).
3. This slice can reach `implementation-ready` without a harvest artifact if
   the recheck confirms the seams above — record that decision explicitly when
   it happens.

## Tests and fixtures (seed)

- N copies have unique IDs, exact offsets and correct internal remaps.
- Owned objects follow copied room; no Scene/camera records copied or modified.
- Linked-door/profile restrictions explained; invalid final copy rolls back all.
- Undo/redo restores IDs and entire batch; 50-copy fixture catches nonfinite
  geometry, ID collisions and excessive repeated compilation.

## Open questions

- Whether opening repeat validates the whole segment batch (all openings on the
  segment) or only the new copies against existing intervals.
- Repeat direction sign handling (negative delta) — allow or normalize.
