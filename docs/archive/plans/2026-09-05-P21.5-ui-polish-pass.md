# P21.5 Plan: UI Polish Pass — Shell Ergonomics, Button Semantics, Inspector Density & Viewport De-Cluttering

**Status:** Consolidated implementation spec (codebase-verified 2026-09-05 — all-themes aesthetic & affordance pass). **Slices 1–5 landed:** Slice 1 (surface-step/buttons/tracks) + Slice 2 (2.1/2.2/2.3/2.5 + §2.6 gizmo detach landed in-tree, owner eye-test pending) + Slice 2B (camera node colors landed in-tree, owner eye-test pending) + Slice 3 (Inspector density + selection isolation + View-menu relocation) + Slice 4 (typography grammar + seven-theme sweep pinned, no `--editor-bg-track` split) + Slice 5 (timeline density — 48px/36px geometry frozen, ghost transport, 11px tabular ruler, collapsed-pill keyboard parity) complete; P21 final acceptance gate pending.
**Authority:** `docs/plans/2026-09-04-P21-unified-project-shell-spatial-reconciliation.md` §P21.5 + `docs/Design-specs/Design-Plan(P21+).md` + `docs/components/shell.md` (P21+ target) + P12 timeline contract. Behavior-beats-pixels per P21 §1: no PNG redefines selection/history/ownership/transforms/topology/timing/scopes/isolation.
**Scope:** Presentation-only. No behavior, ownership, transform-model, topology, timing, persistence, isolation, or navigation change. No new controls, no dead buttons.

> Authority note: Theme authority is `apps/editor/src/lib/editor/theme.svelte.ts` + `styles/tokens.css` (header comment = layer contract) + `styles/scene-palette.ts` (Three.js mirror, contract-tested).

---

## 1. What the draft got right, and what drifted

Verified against source (file:line citations are the buildable truth; paths below are the corrected ones):

1. **Outline disease vs. button invisibility — the all-theme problem.** Inactive buttons previously swung between two extremes: loud accent outlines on everything, or pure transparent ghosts (`background: transparent; border: 0` — e.g. `WorkspaceRibbon.svelte:63`, `CameraPlanToolbar.svelte:94-103`) that render as floating text with zero surface delta against the ribbon. Slice 1.4 fixes both: enclosed tool-group tracks, visible resting control surfaces, mandatory 14px Lucide icons, soft-accent active fills.
2. **Segmented controls — half done.** `[Scene|Camera]` + `[Plan|3D]` are already enclosed `.segmented` groups in `app/WorkspaceRibbon.svelte:30-41`. Only `[Layout|Arrange]` is still a loose `.tool-group.mode-group` in `layout/LayoutDraftToolbar.svelte:64-67` (ribbon mode additionally strips containers at `:115`). Slice 1.2 narrows to unifying that third switch.
3. **Row geometry — already locked, verify-only.** `--editor-project-row-height:36px / --editor-ribbon-height:32px / --editor-status-height:24px` are set in `styles/tokens.css:510-513` (`.project-editor` override; `:root:195-196` still reads `56px/32px` as pre-P21 base), consumed by `app/EditorApp.svelte:2037` grid rows and enforced for status by `styles/editor-shell.css:26`. Row 1 owner is `app/ProjectRow.svelte:205` (inner controls 28px at `:208`), Row 2 is `app/WorkspaceRibbon.svelte:60` (buttons currently **26px** at `:63` — micro-tune to the 28px Row 2 control spec, see Slice 1.1). There is no `app/EditorAppBar.svelte`; the legacy bar lives at `lib/editor/EditorAppBar.svelte` pending P21.1 split-then-delete.
4. **Viewport pills — files misidentified, class misnamed.** No `instruction-banner` exists anywhere in `apps/editor/src`. The floating pills are `.plan-help` (`role="status"`, pill styling) in **two** viewports: `camera-plan/CameraPlanViewport.svelte:172` (string) rendered at `:808`, and `layout/LayoutPlanViewport.svelte:1715-1728`. `app/PlanWorkspace.svelte` is **not** a banner source. Fix both `.plan-help` sites; leave `PlanWorkspace` alone.
5. **Status bar hints — already exist.** `app/StatusBar.svelte:132-142` already renders Plan (`Middle+Drag pan / Scroll zoom / Shift angle snap`) vs 3D (`Alt+Drag orbit / Shift+Drag pan / Scroll zoom`) hints, plus P21.2/P21.3 per-workspace strings at `:108-120` and left/save/right zones at `:124-147`. Slice 2.2 is keep + lock 24px, not add.
6. **Inspector — structure confirmed, density claim overstated.** `EditorInspector.svelte` lives at `lib/editor/EditorInspector.svelte` (1247 lines), delegating to `camera/EditorCameraInspector.svelte`, `app/CameraPlanInspector.svelte`, `EditorLight/Material/Placement/Primitive/TransformInspector` + `fields/EditorNumberField.svelte` (`:8-16`), with routing at `:652` (layout) / `:837` (staging) / `:896` (assets) / `:939` (camera) / `:945` (placement) / `:1043` (empty). Permanent panels confirmed at `:1051-1067` (Camera: Pan/Grid/Floor, hidden only in staging) and `:1069-1083` (Lighting: Bright/Visitor + Ambient/Directional/Fog sliders, unconditional). Transform is **already a 3-column grid** (`EditorTransformInspector.svelte:107-126` Position, `:131-153` Rotation, `:223-251` independent Scale via `.field-grid` at `:314-318`). Real work: compact `EditorNumberField` density + axis chips + tabular-nums (Slice 3.1). **Do not create `components/TransformInputRow.svelte`**. The shared field is `fields/EditorNumberField.svelte` (+ sibling `fields/EditorVec3Field.svelte`).
7. **All-theme surface contrast — not just light mode.** Verified per theme block in `tokens.css`: navy (`#0a141f`→`#0e1a26`), salon (`#140f0d`→`#1f1714`), plum (`#120b1f`→`#1f1334`), moss (`#0f1712`→`#1a2820`) panel→control steps are all under a 5% hex delta — tracks melt into chrome. Correction to the critique: the ribbon background is `--editor-bg-panel-raised` (`--editor-bg-row-2`, `tokens.css:515`), not `--editor-bg-panel`, so the load-bearing step is **panel-raised → control**. Porcelain (`#f6f3ec`→`#e4ddd2`) and the two unlisted themes (synth `#121726`→`#1f273d`, velvet `#1a1814`→`#2a2620`) already separate — verify-only for those three.
8. **Timeline mini-player — density-only.** Reality (`camera/EditorCameraTimelineFrame.svelte`): collapsed **48px** floating mini-player (`:744-753` absolute `bottom:16px` centered pill, `border: --editor-border-normal`, **no red/coral border anywhere**), expanded header **36px** (`:782-789`), transport = scope-capsule + edge-flip + prev/play/next + scrubber + POV/Observer + observer actions (`:684-721`). P21 umbrella §9 + `components/shell.md` pin this: keep transport **above the lanes**, one shared dock geometry, 48px mini-player, lane density `label 120px / ruler 28px / lanes 44/48/34/34/32`, `+View Key` renders live-dock-wide (Edge + Sequence, Plan + 3D; owner decision 2026-09-07 — eligibility gates on disabled, not visibility; relic keeps its Ruler button).
9. **`Tour` is not stray — do not delete.** `LayoutDraftToolbar.svelte:90-92` `Tour` toggles `planView.showTourOverlay` in Layout mode only. It is a legitimate Plan overlay option in the P21 Row 2 capability matrix (Zone C precision/overlay), not authoring-context leakage. Keep.

---

## 2. Execution slices (consolidated)

```text
P21.5 Polish Execution (presentation-only)
├─ Slice 1: Surface-step calibration + Row geometry + Segmented unification + Button Affordance & Tool Tracks
├─ Slice 2: .plan-help removal (2 viewports) + status-bar lock + gizmo single-ending + edge timing pill + §2.6 gizmo continuation (OPEN — to be continued)
├─ Slice 2B: Camera Plan node colors — canvas-invariant tokens (separate mini-slice, immediately after Slice 2; owner eye-test QA) — see [P21.5 Slice 2B brief](2026-09-06-P21.5-slice-2B-camera-node-colors.md)
├─ Slice 3: Inspector density via EditorNumberField + selection isolation to View menu
├─ Slice 4: All-Theme verification sweep & Typography Grammar (landed 2026-09-06 — tiers pinned in `contracts.test.ts`; sweep holds the Slice 1.1 calibration with no `--editor-bg-track` split — no new hexes without owner eye-test)
└─ Slice 5: Timeline density only, P12 geometry frozen (landed 2026-09-06 — ghost transport verified, ruler/timecode tiers + frozen composition + pill keyboard parity pinned in `contracts.test.ts`)
```

Sub-slices land in numeric order: every Slice 1 sub-slice completes before
Slice 2 starts, and so on. The surface-step calibration leads Slice 1
because every later track/button/pill style builds on its token values.

### Slice 1: Calibration, rows, segmented controls, button grammar & tool tracks

#### 1.1 Surface step calibration (panel-raised ribbon → control tracks)
* The load-bearing step is ribbon (`--editor-bg-row-2` = `--editor-bg-panel-raised`, `tokens.css:515`) → `--editor-bg-control`. Calibrate per theme block (theme-aware tokens — never `:root` navy globals beyond the navy block itself):
  * `navy-blue`: control `#0e1a26` → `#121f2e` (lifts off raised `#0d1925`; panel `#0a141f` untouched).
  * `salon-espresso`: control `#1f1714` → `#221a16`.
  * `electric-plum`: control `#1f1334` → `#211538`.
  * `acid-moss`: control `#1a2820` → `#1c2b22`.
  * `porcelain-atelier`: control already `#e4ddd2` — verify-only; keep quiet hairlines (`#ddd6cb` / `#c4bbb0`).
  * `synth-sunset` (`#121726`→`#1f273d`) and `velvet-kodachrome` (`#1a1814`→`#2a2620`): steps already clear — verify-only.
* **Blast radius note:** `--editor-bg-control` feeds every input, panel, menu, and floating toolbar in all themes — not just the ribbon. The QA sweep must cover Inspector fields, menus, pills, and floats in all 7 themes, not just Row 2. If the sweep shows collateral damage, scope a dedicated `--editor-bg-track` token instead of moving the shared control token (Slices 1.4/2.5 then reference `--editor-bg-track`).
* Light-mode 3D viewport stays studio warm gray (`--editor-bg-viewport: #ddd7ce` via `light-dark(#ddd7ce, #071019)`) — already shipped, verify-only. Selection stays brand blue in every theme.

#### 1.2 Geometry — verify, then one micro-tune
* Row 1 = `app/ProjectRow.svelte` (36px via token), Row 2 = `app/WorkspaceRibbon.svelte` (32px), status = `app/StatusBar.svelte` (24px token + `editor-shell.css:26` enforcement). Confirm at runtime: 36 / 32 / 24 with no residual 44px layout shell-bar.
* Micro-tune: `WorkspaceRibbon.svelte:63` buttons read `height:26px; padding:0 7px` — align Row 2 controls to the P21.1 **28px** control height (`controls.css:11` `--editor-control-height:28px`). Inner buttons inside segmented containers stay 24px per spec.
* Hairlines: Row 1 / Row 2 bottom borders use `var(--editor-border-subtle)`; ribbon currently uses `--editor-border-normal` at `WorkspaceRibbon.svelte:60` — reconcile to subtle per spec.

#### 1.3 Segmented controls — unify the third switch only
* Keep `WorkspaceRibbon.svelte:30-41` Zone A groups as-is (fixed `flex:0 0 240px` at `:61`).
* Bring `LayoutDraftToolbar.svelte:64-67` `[Layout|Arrange]` into the same `.segmented` grammar (container + `aria-pressed`/`.active` + 24px inner buttons) so all three switches — `[Scene|Camera]`, `[Plan|3D]`, `[Layout|Arrange]` (Scene Plan only) — share one visual language. Preserve the ribbon-mode container strip (`:115-118`) semantics: the **Ribbon** provides the enclosure in ribbon mode; the floating toolbar keeps its own enclosure when floating.
* Accessibility: keep `role="group"` + `aria-label` on each group; keep `aria-pressed` (current pattern).

#### 1.4 All-theme button affordance, tool tracks & mandatory icons
* **The affordance fix (all themes):** inactive buttons must neither shout with bright wireframe outlines nor vanish into the ribbon as borderless text. Resting state gets a real surface; active state gets the soft-accent fill (never a loud bare stroke).
* **Tool tracks (`.tool-group`):** Zone B authoring groups (`LayoutDraftToolbar` room/opening tools, `CameraPlanToolbar` camera tools, `EditorViewportToolbar` transform/space groups — all currently divider-separated transparent buttons) sit inside cohesive filled tracks:
  ```css
  .tool-group {
    display: inline-flex;
    align-items: center;
    height: 28px;
    padding: 2px;
    background-color: var(--editor-bg-control);
    border: 1px solid var(--editor-border-subtle);
    border-radius: 6px;
    gap: 1px;
  }
  ```
  (Track background comes from the Slice 1.1 calibrated `bg-control`, which must lift clearly off the `bg-panel-raised` ribbon.)
* **Standalone buttons:** individual buttons outside a track (`Select`, `Add Asset`, `View ▾`) get a subtle resting presence:
  ```css
  .ribbon-btn {
    height: 28px;
    padding: 0 8px;
    background-color: var(--editor-bg-control);
    border: 1px solid var(--editor-border-subtle);
    border-radius: 5px;
    color: var(--editor-text-primary);
    font-size: 12px;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .ribbon-btn:hover {
    background-color: var(--editor-bg-hover);
    border-color: var(--editor-border-normal);
  }
  ```
* **Mandatory 14px Lucide icons:** every Zone B command button pairs its label with a glyph — `Square` (Rect Room), `Pentagon` (Poly Room, owner-ratified over `Spline`), `DoorOpen` (Door), `Grid2x2` (Window, owner-ratified over `PanelTopOpen`), `Move` / `RotateCw` / `Scaling`, `Camera`/`Video` (Add Camera), `Waypoints` (Connect). Precedent exists (`EditorViewportToolbar.svelte:2` already imports `Move`, `RotateCw`, `Scaling`, `Video`, `Eye`; `CameraPlanToolbar` and `LayoutDraftToolbar` are fully text-only today). Correction: **`Polygon`/`VectorPolygon` do not exist in `lucide-svelte@1.0.1`** (verified against `node_modules`; 1.0.1 is also the latest published version, so no install can provide them — the pre-authorized upgrade is a no-op) — `Spline` is the Poly Room glyph; a `Polygon`/`VectorPolygon` import fails `check`.
* **Pinned (owner-ratified):** In `LayoutDraftToolbar.svelte`, replace all bare-text buttons (Rect Room, Poly Room, Door, Window) with lucide-svelte icons (`Square`, `Pentagon`, `DoorOpen`, `Grid2x2`) paired with text labels at `gap: 6px` inside the `.tool-group` container. Disclosure/summary toggles (Snap `<details>`, View menu) are exempt from the mandate.
* **Universal 4-tier state grammar:**
  * Resting: `var(--editor-bg-control)` fill + `var(--editor-border-subtle)` + `var(--editor-text-primary)`.
  * Hover: `var(--editor-bg-hover)` + `var(--editor-border-normal)`.
  * Active / toggled on: `var(--editor-accent-soft)` fill + `var(--editor-accent-border)` + `var(--editor-text-primary)`.
  * Primary CTA (`▶ Preview`, `Save`): solid `var(--editor-accent)` with high-contrast light text.

### Slice 2: Canvas de-cluttering, status bar, gizmo & edge timing pill

#### 2.1 Remove `.plan-help` pills from both viewports
* Delete: `camera-plan/CameraPlanViewport.svelte:808` (+ string logic `:161-173`) and `layout/LayoutPlanViewport.svelte:1715-1728`. Leave `app/PlanWorkspace.svelte` and `app/CameraPlanWorkspace.svelte` shells untouched.

#### 2.2 Status bar — lock, don't build
* Content already correct (`StatusBar.svelte:108-147`). Work: enforce exactly **24px** standalone (reconcile `:155-156` `min-height:1.7rem` to token). Keep hints informational only.

#### 2.3 `Tour` — keep
* `Tour` (`LayoutDraftToolbar.svelte:91`) stays as the Layout-mode Plan overlay toggle in Zone C. Keep.

#### 2.4 Gizmo single-ending (TransformControls)
* **Fix:** hide negative-end tip meshes per axis (Move arrow / Scale grip at the `+` tip stays), synchronously beside `applyEditorGizmoPalette(transformHelper)` in `gizmo/EditorTransformControlsHost.svelte:38-42` (module-init code, not a mount effect). Match tip meshes by local-position sign, skip axis lines at origin, **never touch the invisible `picker` hierarchy** (hit-testing/snap/hover/active untouched — host comment `:39-40` already pins this contract).
* **Home:** walker lives in `styles/scene-palette.ts` beside `applyEditorGizmoPalette` (same cosmetic-only contract: materials/visibility only, geometry/pickers untouched; keeps the `scene-palette.test.ts` token-sync story intact).
* **Overlap:** additive only — P21.2 keeps the gizmo untouched, P3.2 recolored materials only. Test: unit asserting all axes remain present and draggable (presence, not pixels) + existing gizmo/selection suites stay green.

#### 2.5 Compact camera edge timing pill (replaces the stacked debug strings)
* **Home (corrected):** the text is composed in `layout/plan-camera-projection.ts:181-196` (`buildPlanCameraAuthoringProjection` emits two `camera-timing-label` text primitives per edge — `${fromLabel}→${toLabel} ${dur}s[ auto]` at midpoint offsets `[0,-16]` / `[0,18]`), rendered as bare 10px SVG text by `layout/PlanSvg.svelte:277`. `camera-plan/CameraPlanViewport.svelte` only renders primitives — it owns no label strings, so the fix lands in the projection + renderer, not the viewport.
* **Remove:** full node-name strings, the `auto` word, and the stacked two-line layout (both offsets collapse to a single midpoint anchor `[0,0]`).
* **Replace with one pill per connection** (single primitive, centered on the spline midpoint): durations only, `toFixed(1)` + `s` as today (Image 8's `4.00 s` spacing is generator detail) — equal directions → `4.0s`; differing → `4.2s · 3.8s` (forward · reverse); non-finite stays `—` (current fallback preserved). Authored vs automatic stays distinguishable per the `camera-tour.md` contract through a visual channel: automatic segments muted + dotted underline + tooltip, authored segments solid. Node names, direction breakdown, and derived speed live only in the connection Inspector (Slice 3.2; `CameraPlanInspector.svelte:278-279` already shows From/To).
* **Styling:** ~18px height, `radius 4px`, 11px/600 tabular text on invariant pill ink (reviewer contrast fix 2026-09-07 — the canvas is paper in every theme, so pill ink never follows the chrome accent): unselected → white fill + slate border + charcoal ink (reads on paper and room floors); selected edge → deep-sapphire fill + white ink + electric-blue perimeter tied to the route. Automatic segments keep the dotted underline (3px offset, 1.5px thick) + tooltip; the old `accent fill + white text` grammar failed contrast (3.3:1) and theme-clashed (terracotta in porcelain). `pointer-events:none` preserved.
* **Plumbing:** label primitives carry no selection/authored state today (only the sibling `connection.selected` does) — extend the primitive (selected flag + per-segment authored flags) and teach the renderer to draw the rounded-rect badge behind centered text (`PlanSvg` text primitives are bare `<text>` today). No new selection, timing, or motion logic; `resolveCameraConnectionTiming` and per-direction Inspector authoring are untouched.
* **Pinned amendments (same slice, presentation-only):** update `docs/components/camera-tour.md:90-91` (both-directions labeling → compact pill grammar) and `tests/lib/editor/layout/plan-camera-projection.test.ts:298-310` (two verbose strings → single-pill expectations). Without these the slice contradicts a contract line and fails a pinned test.

#### 2.6 Gizmo single-ending continuation — OPEN (to be continued)

* **Status:** the §2.6 detach fix (`applyEditorGizmoSingleEnding` detaches + disposes the six negative-end tips; lifecycle test covers frames + mode cycles) landed in-tree — owner eye-test in a fresh build remains the close gate (Move and Scale show exactly one tip per axis, hover/active/drag unchanged).
* **Root cause (verified against installed three 0.175.0):** `TransformControlsRoot.updateMatrixWorld()` rebuilds handle state **every rendered frame**, including `handle.visible = true` on every picker/gizmo/helper handle of the active mode (`TransformControls.js:1487`). One-shot `visible = false` flags die on the first frame. The unit test passes only because it never runs `updateMatrixWorld`; a lifecycle repro (walk → attach → frames → mode cycle) shows hidden tips dropping to zero.
* **Fix direction (not a second walker run):** detach — don't hide. At mount, remove the six negative-end tip meshes from their gizmo parents (translate cones + scale grips, X/Y/Z) instead of flipping `visible`. three never re-adds children per frame, so detachment survives; raycast stays intact because picking resolves against the invisible picker subtree, which the walker never enters. Dispose detached geometries. Cosmetic-only contract unchanged (materials/visibility-flags of everything else untouched, no geometry/picker edits).
* **Test rewrite:** the committed `gizmo-single-ending.test.ts` asserts the failed mechanism (`visible === false`, "nothing removed"). Rewrite it lifecycle-aware: build real controls → apply → attach object → run root `updateMatrixWorld` frames → cycle translate/scale/rotate → assert six negative tips absent from the gizmo subtree, six positive tips present and visible, picker subtree byte-identical. The old assertions must go, not be extended.
* **Acceptance:** owner eye-test in a fresh build — Move and Scale show exactly one tip per axis (+ end); hover/active/drag behavior unchanged. No new unit-test-only gate replaces the eye-test.

### Slice 3: Inspector density + selection isolation (no new row component)

#### 3.1 Compact XYZ — restyle the shared field, don't fork it
* Reuse `fields/EditorNumberField.svelte` (+ `EditorVec3Field` where used): compact **28px** row variant with inline axis chip (18×18px, 10px/700, radius 3px; X `rgba(240,82,82,.15)/#F05252`, Y `rgba(69,200,120,.15)/#45C878`, Z `rgba(59,130,246,.15)/#3B82F6` — consistent with `--editor-axis-*`), borderless tabular input (`font-variant-numeric:tabular-nums; font-size:12px`).
* Apply via the existing `.field-grid` 3-column rows in `EditorTransformInspector.svelte` (Position/Rotation/independent Scale) and the layout X/Z/Yaw rows in `EditorInspector.svelte:719,725,731` + `:865,871,877`. Fold the standalone `.axis-legend` (`EditorTransformInspector.svelte:99-103`) into the per-field chips. Keep uniform/independent scale toggle semantics (placement contract) — density only.

#### 3.2 Selection isolation — remove permanent panels, route strictly
* Delete the always-mounted sections: `EditorInspector.svelte:1051-1067` (Camera Pan/Grid/Floor, hidden only in staging) and `:1069-1083` (Lighting Bright/Visitor + Ambient/Directional/Fog sliders, unconditional). Zero selection shows the primer only.
* Preserve existing routing (`:652` layout CAD / `:837` Arrange / `:896` assets / `:939` camera incl. `CameraPlanInspector` vs `EditorCameraInspector` / `:945` placement / `:1043` empty). Camera node → pose/FOV/look-at/timing; connection → undirected endpoints + bidirectional duration + derived speed + spline type; 3D object → name + Transform + Placement + PBR material. Gate the `.place-tools` 2D drafting grid to Scene Plan Layout only — never Scene 3D or Camera.

#### 3.3 Relocation target — View menu, never status-bar controls
* Floor color, session ambient sliders, viewport grid toggles, and the Pan toggle move into the existing **View menu** (`EditorViewportToolbar.svelte:170,385` owns the Scene 3D View menu; add Plan equivalents if missing). Status bar takes strings/hints only — authoring controls there violate its documented contract.

### Slice 4: All-theme verification sweep & typography grammar

#### 4.1 Seven-theme verification sweep
* Re-check the Slice 1.1 steps against the finished Slices 1–3 chrome: tracks, buttons, pills, Inspector fields, menus, and floats must read clearly in all 7 themes (4 dark blocks recalibrated in Slice 1.1; porcelain/synth/velvet verified). Any regression here reopens Slice 1.1 (including the `--editor-bg-track` split), not a local override.

#### 4.2 Typography & tabular numerals (all themes)
* Apply `font-variant-numeric: tabular-nums` to all coordinates, dimensions, angles, and timecodes.
* Enforce three tiers across the Inspector:
  * **Section headers:** `11px`, uppercase, `font-weight: 600`, `letter-spacing: 0.05em`, `color: var(--editor-text-muted)` (current headers read `0.95rem/650` at `EditorInspector.svelte:1097` — align).
  * **Property labels:** `12px`, `font-weight: 400`, `color: var(--editor-text-secondary)`.
  * **Values / inputs:** `12.5px`, `font-weight: 500`, tabular nums, `color: var(--editor-text-primary)`.

### Slice 5: Timeline — density only, P12 geometry frozen

* **Frozen:** collapsed **48px** mini-player, expanded header **36px**, transport **above the lanes**, one shared dock across Camera Plan/3D, scope/transport semantics verbatim. There is no red/coral border to strip (collapsed pill uses `--editor-border-normal` + shadow at `EditorCameraTimelineFrame.svelte:744-753`); do not re-introduce one, do not re-dock into the status bar, do not resize to 40px, do not swap the scope-capsule/edge-flip/transport/scrubber/POV-Observer composition (`:684-721`) for generic icons.
* **Allowed:** lane density per P21.1 (`label 120px, ruler 28px, lanes 44/48/34/34/32`), 11px tabular ruler timecodes with playhead pinned to current time, quiet ghost transport buttons, collapsed-pill polish (spacing, contrast, keyboard/focus) within the existing floating geometry. `+View Key` renders live-dock-wide (owner decision 2026-09-07 — disabled gates eligibility, not visibility).

---

## 3. Corrected file map (replaces draft §3)

```text
apps/editor/src/lib/editor/
├── styles/
│   ├── tokens.css                        → Slice 1.1 control-step calibration + verify 36/32/24 (.project-editor:510-513); porcelain/synth/velvet verify-only
│   ├── controls.css                      → Slice 1.4 .tool-group track styling; 4-tier affordance; enclosed segmented grammar
│   ├── editor-shell.css                  → 24px status enforcement (:26) + grid rows with EditorApp.svelte:2037
│   └── scene-palette.ts                  → single-end walker beside applyEditorGizmoPalette (:60-86)
├── theme.svelte.ts                       → registry only (draft path styles/theme.svelte.ts is wrong); no per-polish logic
├── app/
│   ├── ProjectRow.svelte                 → Row 1 verify 36px (:205), inner 28px (:208) [draft's app/EditorAppBar.svelte is wrong]
│   ├── WorkspaceRibbon.svelte            → Row 2 verify 32px (:60); tool-group tracks + icons; 26px→28px button tune (:63)
│   ├── StatusBar.svelte                  → lock 24px standalone (reconcile :155-156 1.7rem); content already done
│   ├── PlanWorkspace.svelte              → shell only (draft path plan/PlanWorkspace.svelte is wrong); no banner work
│   └── CameraPlanWorkspace.svelte        → Camera Plan shell; no banner work
├── EditorInspector.svelte                → delete :1051-1083 permanent panels; gate .place-tools; keep routing
├── EditorTransformInspector.svelte       → keep .field-grid 3-col; fold axis legend into chips
├── fields/
│   ├── EditorNumberField.svelte          → compact 28px axis-chip variant + tabular-nums (NO new TransformInputRow)
│   └── EditorVec3Field.svelte            → same treatment where used
├── gizmo/
│   └── EditorTransformControlsHost.svelte → single-end walker call at :38-42 (sync init, picker untouched)
├── layout/
│   ├── LayoutPlanViewport.svelte         → remove .plan-help :1715-1728
│   ├── LayoutDraftToolbar.svelte         → [Layout|Arrange] segmented unification (:64-67); tool-group track + icons; keep Tour (:91)
│   ├── plan-camera-projection.ts         → single-pill timing emission (:181-196)
│   └── PlanSvg.svelte                    → pill badge rendering for camera-timing style (:277; text primitives are bare <text> today)
├── camera-plan/
│   └── CameraPlanViewport.svelte         → remove .plan-help :808 (strings :161-173)
├── EditorViewportToolbar.svelte          → tool-group tracks + icons (partial precedent: :2 imports, Video/Eye in use); View menu relocation home (:170,385)
├── camera-plan/CameraPlanToolbar.svelte  → tool-group track + icons (fully text-only today: :45-87, transparent :94-103)
└── camera/
    └── EditorCameraTimelineFrame.svelte  → density only; 48px/36px geometry frozen (:744-753, :782-789)
```

---

## 4. Verification checklist (Definition of Done)

* [ ] Rows measure 36 / 32 / 24 with no residual 44px bar; ribbon buttons 28px.
* [ ] No transparent ghost buttons in Row 2: every tool sits in a visible `.tool-group` track or carries its own resting surface; every Zone B command button pairs its label with a 14px Lucide icon (disclosures/menus exempt).
* [ ] All three switches (`Scene|Camera`, `Plan|3D`, `Layout|Arrange`) share one enclosed segmented grammar; no inactive button carries an accent outline.
* [ ] No `.plan-help` pill in either viewport; hints live only in the status-bar center zone.
* [ ] Camera Plan edges show one midpoint pill each (`4.0s`, or `4.2s · 3.8s` when directions differ; auto segments dotted + tooltip, never the word `auto`); unselected pill is white/slate/charcoal, selected edge pill is sapphire fill + white ink + blue perimeter (invariant ink, never the chrome accent). No node names or arrows on canvas.
* [ ] Gizmo: one arrowhead / grip-cube per axis tip; all axes present and draggable; hover/active/pickers intact; `three` bump re-check noted.
* [ ] Inspector: XYZ rows are single-line 28px with RGB chips + tabular nums; no permanent Camera/Lighting sections; no 2D Place tools outside Scene Plan Layout; empty state shows primer only.
* [ ] Viewport lighting/floor/grid/pan live in View menu(s); status bar has zero authoring controls.
* [ ] Surface steps read clearly in all 7 themes (4 dark blocks recalibrated; porcelain/synth/velvet verified); Inspector type grammar + tabular nums throughout.
* [ ] Timeline: 48px collapsed / 36px header / transport-above-lanes preserved; ruler 11px tabular; `+View Key` live-dock-wide (disabled gates eligibility).
* [ ] Full Vitest + `check` + `build` (editor+museum) + `verify:visitor-bundle` + preview-surface import-closure gate green; six-PNG comparison + axe/contrast/keyboard/reduced-motion sweep per P21 acceptance gate. No `/museum` chunk contains editor code (visitor isolation).

---

## 5. Draft questions — answered (no implementer re-ask needed)

1. **Inspector decomposition:** one large `lib/editor/EditorInspector.svelte` (1247 lines) routing into `camera/EditorCameraInspector.svelte`, `app/CameraPlanInspector.svelte`, `EditorLight/Material/Placement/Primitive/TransformInspector` + `fields/*`. Permanent Camera/Lighting panels are inline sections (`:1051-1083`), not separate debug components — delete inline.
2. **Transform fields:** shared `fields/EditorNumberField.svelte` (props `label/value/step/min/fractionDigits/oncommit`, `<input type="number">` at `:80-89`) + `EditorVec3Field.svelte`. X/Y/Z are already shared-component rows in `EditorTransformInspector` — restyle, don't replace. No `NumericInput`, no `TransformInputRow` (do not create).
3. **Banner source:** neither draft candidate. `.plan-help` in `camera-plan/CameraPlanViewport.svelte:808` and `layout/LayoutPlanViewport.svelte:1715-1728`. No `instruction-banner` class exists.
4. **Icons:** `lucide-svelte@1.0.1` is already wired (timeline, tree, toolbars). `Square`, `Spline` (Poly Room — see below), `DoorOpen`, `PanelTopOpen`, `Waypoints`, `Move`, `RotateCw`, `Scaling`, `Camera`/`Video` all resolve; **`Polygon` and `VectorPolygon` do not exist** in the installed package, and 1.0.1 is the latest published version, so no install can provide them (pre-authorized upgrade evaluated 2026-09-05 — no-op).

Two Minor Advisories for the Implementer
Poly Room Icon Alternative (Spline vs. Pentagon):

Spline is verified and 100% safe in 1.0.1. However, if visual QA feels Spline looks too much like a curve path rather than a closed polygon room, check if Pentagon or Hexagon exists in that node_modules export. If they do, either is an acceptable alternative; if not, keep Spline as pinned.

Native SVG for the Edge Pill (PlanSvg.svelte):

When teaching PlanSvg.svelte to render the rounded-rect timing badge behind text, use native SVG <rect rx="4"> and <text text-anchor="middle" dominant-baseline="central">. Avoid <foreignObject> here, as HTML foreignObjects in SVG canvases often suffer from subpixel blur and transform-matrix scaling bugs across Safari and Firefox.
